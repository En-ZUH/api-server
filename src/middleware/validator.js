// 'use strict';

// module.exports = (request, response, next) => {
//     let id = request.params.id;
//     if (!id)
//         throw new Error("ID doesn't exist");
//     else
//         next();
// }
